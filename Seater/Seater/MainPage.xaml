<?xml version="1.0" encoding="utf-8" ?>
<MasterDetailPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Seater"
             x:Class="Seater.MainPage"
                  Title="Menu"
                  MasterBehavior="Popover"
                  NavigationPage.HasBackButton="False">
    <MasterDetailPage.Master>
        <ContentPage Padding="10" BackgroundColor="Gray" Title="Menu" Icon="reserve.png">
            <ContentPage.Content>
                <StackLayout Margin="5,30,5,5">
                    <Label Text="Menu"></Label>
                    <Button x:Name="search" Text="Search" TextColor="White" BackgroundColor="Black" Clicked="menu_search_button"></Button>
                    <Button x:Name="reserve" Text="Reservation" TextColor="White" BackgroundColor="Black" Clicked="menu_reserve_button"></Button>
                    <Button x:Name="profile" Text="Profile" TextColor="White" BackgroundColor="Black" Clicked="menu_profile_button"></Button>
                </StackLayout>
            </ContentPage.Content>
        </ContentPage>
    </MasterDetailPage.Master>
    <MasterDetailPage.Detail>
        <NavigationPage>
        </NavigationPage>
    </MasterDetailPage.Detail>
</MasterDetailPage>
